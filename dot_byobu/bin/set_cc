#!/bin/bash

ocolour="colour72"
if [[ $# -ge 1 ]] ; then
    ocolour=$1
fi

tmux set -g cursor-color colour9
sleep 0.5
tmux set -g cursor-color colour11
sleep 0.5
tmux set -g cursor-color colour9
sleep 0.5
tmux set -g cursor-color $ocolour

exit 0
