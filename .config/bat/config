--decorations="always"
--color="always"
--paging="auto"
--tabs="4"

# Set the theme to "TwoDark"
#--theme="Monokai Extended"
# molo is Monokai Extended but with a more visible lineHighlight
--theme="molo"

# Show line numbers, Git modifications and file header (but no grid)
#--style="numbers,changes,header"
#--style="plain"

# Use italic text on the terminal (not supported on all terminals)
#--italic-text=never

# Add mouse scrolling support in less (does not work with older versions of "less")
# dont use -F without -X - and with -X there is no mouse support ...
# is this still the case with current version of less ?

#--pager="less -iR -K -x4 -c -~"
--pager="less"

# Use C++ syntax (instead of C) for .h header files
--map-syntax h:cpp

# Use "gitignore" highlighting for ".ignore" files
--map-syntax .ignore:.gitignore

# bat cache --clear
# bat cache --build

# add dir: ~/.config/bat/syntaxes/sublime-plaintext-syntax
# and in this dir, add file: plaintext.sublime-syntax
# and in this file:
#
# https://www.sublimetext.com/docs/syntax.html
# https://www.gnu.org/software/bash/manual/bash.html
#name: Plain Text
#scope: source.shell.bash
#version: 2
#
#extends: Packages/ShellScript/commands-builtin-shell-bash.sublime-syntax
#
#file_extensions:
#  - txt

