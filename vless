#!/bin/bash

if [[ -z "$VLESS" ]] ; then
    if [[ -z "$VISUAL" ]] ; then
        vimless $@
    elif [[ "$VISUAL" == "vim" ]] ; then
        vimless $@
    else
        nvless $@
    fi
elif [[ "$VLESS" == "vless" ]] ; then
    vimless $@
else
    nvless $@
fi
